# Example Streamlit Deephaven app

## Quickstart

To use, create a venv and install streamlit-deephaven, then run the app:

```
python -m venv .venv
source .venv/bin/activate
streamlit run deephaven_app.py
```
